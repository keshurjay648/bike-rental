{
  "name": "bike-rental-app",
  "version": "1.0.0",
  "description": "Full stack bike rental application",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start",
    "install:all": "cd backend && npm install"
  }
}
